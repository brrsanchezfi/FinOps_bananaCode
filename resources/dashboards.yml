# =============================================================================
# Dashboards Lakeview.
#
# Los archivos apuntan a `dashboards/<target>/`, que estan VERSIONADOS y se
# generan con:
#
#     python scripts/dashboards.py generate
#
# No hay paso de build previo al deploy: `databricks bundle deploy` funciona
# directamente sobre el repositorio. Si cambias el SQL o el layout, edita
# `scripts/dashboards.py`, regenera y commitea (CI verifica que no haya deriva).
#
# `warehouse_id` es obligatorio: definirlo en el bloque `variables` del target
# correspondiente en databricks.yml, o pasarlo con
#     databricks bundle deploy -t prd --var="warehouse_id=abc123"
# =============================================================================

resources:
  dashboards:
    finops_ejecutivo:
      display_name: "[FinOps] Vista ejecutiva (${bundle.target})"
      file_path: ../dashboards/${bundle.target}/finops_ejecutivo.lvdash.json
      warehouse_id: ${var.warehouse_id}
      parent_path: ${workspace.root_path}/dashboards

    finops_costos:
      display_name: "[FinOps] Costos y chargeback (${bundle.target})"
      file_path: ../dashboards/${bundle.target}/finops_costos.lvdash.json
      warehouse_id: ${var.warehouse_id}
      parent_path: ${workspace.root_path}/dashboards

    finops_optimizacion:
      display_name: "[FinOps] Optimizacion y gobierno (${bundle.target})"
      file_path: ../dashboards/${bundle.target}/finops_optimizacion.lvdash.json
      warehouse_id: ${var.warehouse_id}
      parent_path: ${workspace.root_path}/dashboards
